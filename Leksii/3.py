def имя(n): # функция
Модули 
import modull - обращение к модулю(к файлу в котором функции). .
Print (modull.max(5,9)) - вызов функции мах из модулл 

from modull import * - импорт всех функций из модуля или заменать * на имя функции
